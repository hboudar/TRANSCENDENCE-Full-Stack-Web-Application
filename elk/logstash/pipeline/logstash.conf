input {
  beats {
    port => 5044
    ssl_enabled => true
    ssl_certificate => "/usr/share/logstash/config/certs/logstash/logstash.crt"
    ssl_key => "/usr/share/logstash/config/certs/logstash/logstash.key"
    ssl_certificate_authorities => ["/usr/share/logstash/config/certs/ca/ca.crt"]
    ssl_client_authentication => "required"
    ssl_peer_metadata => false
  }
}

filter {
  # 1️⃣ Parse Fastify logs (root-level JSON)
  json {
    source => "message"
    skip_on_invalid_json => true
  }

  # 2️⃣ Only enrich api_request logs
  if [event] == "api_request" {
    mutate {
      copy => { "service" => "service.keyword" }
      copy => { "method"  => "method.keyword" }
      copy => { "path"    => "path.keyword" }
    }
  }
}

output {
  elasticsearch {
    hosts => ["https://elasticsearch:9200"]
    user => "logstash_internal"
    password => "${LOGSTASH_INTERNAL_PASSWORD}"
    index => "transcendence-logs-%{+YYYY.MM.dd}"
    ssl_enabled => true
    ssl_certificate_authorities => ["/usr/share/logstash/config/certs/ca/ca.crt"]
    ssl_verification_mode => "full"
  }

  stdout { codec => rubydebug }
}
